# Reviewer Action Matrix

Status reflects the canonical manuscript `paper/acl_latex.tex` and the frozen repository state on 2026-08-31, after the independent audit in `reviews/claude_independent_audit/`.

| Requested action | Status | Resolution or evidence | Remaining boundary |
|---|---|---|---|
| Define the attack strategies precisely, especially role prompting | Addressed | Section 4.2 gives binding structural definitions for SymbolicMasking, ScenarioNesting, and RolePrompting; Appendix A restates the auditor contract. | No additional strategy is claimed or needed for the present design. |
| Clarify whether the bank is target-conditioned | Addressed | The generator objective is explicitly target-independent; the frozen manifest records no target model and no target signal during generation. | None for the frozen bank. |
| Explain GEPA scoring and weights | Addressed | Corrected after audit: the persisted harness state holds a single seed program with no parent and a null Pareto summary, so no prompt evolution occurred. Section 4.1 and Appendix B now describe a fixed, hand-specified, target-independent instruction validated against a 13-constraint metric (30 evaluations, USD 0.030, no accepted revision). | The metric validates probe validity; it is a diagnostic, not an optimizer. |
| Specify auditor independence and hard gates | Addressed | Section 4.3 identifies the DeepSeek primary and GPT-5 Mini secondary auditors, their cascade, hard acceptance rule, schemas, and failure behavior. | Because the secondary auditor saw only primary passes, full inter-auditor agreement is not estimable. |
| Add human validation of model-based scores | Prepared at the requested scale; labels pending | Rebuilt after audit: 180 pair-model jobs / 360 response items per annotator = **11.9%** of the 1,512-job grid, exactly 5 jobs in each of 36 cells. The v1 90-job (5.95%) package is marked obsolete and was never labelled. Instrument corrections: language-competence requirement, symmetric best-fit tie-break, borderline and cannot-read flags, ≥10-position twin separation, familiarization set with no answer key, prespecified adjudication. | No human result is reported until both workbooks are returned. At 60 jobs per model the study validates judge accuracy, chiefly on Qwen; it cannot re-estimate the gap for GPT-OSS or Nemotron. |
| Define semantic/direct refusal and flip outcomes before analysis | Addressed | Section 3 defines the ordinal score, refusal, refusal gap, forward/reverse flips, and critical flips before results. | None. |
| Use the pair, not individual responses, as the experimental unit | Addressed | Pair ID is explicit as the resampling and inference unit; bootstrap, exact paired tests, randomization, and clustered GEE preserve pairing. Appendix E adds a cluster-robust sensitivity that groups lexically similar prompts. | Pairs are not fully independent: shared scenario scaffolding means the GPT-OSS–Nemotron contrast does not survive cluster-robust resampling, and the manuscript now says so. |
| Strengthen reproducibility | Addressed | Frozen identifiers, SHA-256 hashes, model slugs, decoding settings, seeds, fallback provenance, analysis scripts, and a reproducibility manifest are included. The audit found the tracked generator artifact was the wrong one; the instruction whose SHA-256 the bank manifest records is now committed, and `scripts/verify_generator_artifact.py` checks the correspondence. | Raw harmful content remains gated: the bank, the frozen run, and the annotation workbooks stay out of the repository, so full regeneration needs gated access. The manuscript states this. |
| Add a judge-independent check | Addressed, and re-scoped after audit | Phase C uses only stored response lengths under prespecified thresholds and reproduces the directional ordering. Section 5.3 now decomposes it by judged outcome: 65:0, 22:2 and 12:58 of the shapes come from pairs scored 0 in both languages. | Length corroborates a language-conditioned change in response behavior, of which the flip component is safety-relevant; it is not an independent measure of harmfulness. |
| Replace or cross-check the Gemini response judge | Addressed within budget | All 1,944 responses in the outcome-independent shared subset were re-judged by GPT-5 Mini with the identical rubric; ordering is preserved, while absolute severity calibration changes. Per-condition agreement is now reported (Appendix F). | Not a complete re-judge of all 3,024 responses, and not an uninvolved family: GPT-5 Mini was also the secondary certification auditor and the fallback judge. The judge also sees the prompt and a language tag, and the re-judge replays that cue, so it cannot detect language-conditioned leniency. Both are stated in the manuscript. |
| Position the work against current safety benchmarks | Addressed | Related work and Table 1 compare HarmBench, JailbreakBench, StrongREJECT, LinguaSafe, IndicJR, Banerjee et al. (2025) and IndicAlignProbe. Added after audit: Banerjee et al. (2025) matched-pair code-mixed work, Aswal & Jaiswal (2025) Hinglish red-teaming, Yong et al. (2023), and an explicit discussion of IndicJR's opposite-direction romanization finding. Novelty is narrowed to per-item certified equivalence plus model-resolved heterogeneity. | Comparisons are protocol-level, not empirical benchmark leaderboards. |
| Narrow the scope of the title and claims | Addressed | The manuscript limits claims to three models, one English/Hinglish-RH setting, four harm categories, and the frozen synthetic probe distribution. After audit the "three distinct regimes" claim was replaced: Qwen is robustly distinct from both other targets, and GPT-OSS and Nemotron are not reliably separated. The score-0 construct is renamed *non-assistance*, since none of Qwen's 115 RH score-0 responses is a terse refusal. | No universal multilingual or deployment-prevalence claim is made. |
| Add an unusual-English out-of-distribution control | Deferred | The manuscript marks this experiment as unrun and now states plainly that without it a register-specific effect cannot be separated from generic off-distribution sensitivity. | Requires new target inference and scientific/budget approval. The audit ranks it the only paid experiment worth authorizing, at roughly USD 5–15, and only with a committed preregistration. |
| Add a certification ablation or estimate selectivity | Ablation not identifiable; partial selectivity now available | The frozen attempt log has six records and cannot reconstruct the funnel. The committed generator artifact's `metric_log.jsonl` does record 30 audited candidates with per-constraint failure reasons and 24 acceptances under the frozen objective. | That is a development-set distribution, not the bank-build funnel, and must be labelled as such. A new ablation stays deferred. |
| Expand languages and target models | Future work | Explicitly stated as a limitation. | Consequential new experimental design and paid inference would be required. |

## Items the audit raised that were not previously tracked

| Concern | Status | Resolution |
|---|---|---|
| Is the effect register-specific or generic out-of-distribution sensitivity? (original R2) | Still exposed | Named explicitly in Limitations. Only the deferred unusual-English control can answer it. |
| Residual family overlap across pipeline stages (original R3) | Partially resolved | Generation and primary judging are Gemini; certification is DeepSeek plus GPT-5 Mini; the re-judge is GPT-5 Mini. Stated in Limitations. A fully uninvolved judge family remains future work. |
| The response judge is not blind to language | Disclosed | The judge message carries the prompt and a language tag, and Phase D replays it. Stated in Section 4.4 and Limitations. |
| Asymmetric response truncation at the token cap | Resolved | Reported with a sensitivity analysis: gaps move to 43.36, 4.73 and −0.50 pp. |
| Bowker symmetry results computed but unreported | Resolved | Now reported, including Nemotron's nominal rejection at p = .019. |

## Submission interpretation

The empirical Phase A--D claims are complete and reproducible. The manuscript is a submission-ready draft, but the planned human-validation result remains intentionally blank until two independent annotators return labels. The paper does not imply that a prepared protocol is completed evidence.
